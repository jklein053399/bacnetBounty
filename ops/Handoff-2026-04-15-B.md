# Handoff — 2026-04-15 Session B (→ Session C)

## Session B deliverables

- **Phase 4 commit:** `682f624 Phase 4: AHU device class — 3 instances verified on wire`
- **Phase 5 commit:** `ba012ea Phase 5: VAV device class — 20 instances; 28 devices on wire`
- **Devices on wire:** **28 complete** (3 E-Mon + ONICON gas + ONICON water + 3 AHU + 20 VAV)
- **Verify scripts passing:** 4/4 scripts, **67/67 total checks** (4 + 7 + 28 + 28)

## State at end of Session B

### Tree
```
bacnet-bounty/
├── .claude/
│   ├── CLAUDE.md                              (current)
│   ├── references/
│   │   ├── 01_...Architecture_and_Scope.md
│   │   ├── 02_..._Device_and_Points_Reference.md
│   │   ├── 03_..._Phase1_Handoff_Resolutions.md
│   │   ├── 04_..._Phase2_Resolutions.md         (updated with Flag E: AV/BV override)
│   │   └── hbt-bms-E-Mon-Class-3200-....pdf
│   └── skills/                                 (untracked)
├── simulator/
│   ├── __main__.py                            28-device manifest, warnings filter
│   ├── config.py                              +AHUConfig, +VAVConfig, validators
│   ├── logging_config.py                      unchanged
│   ├── site_model.py                          +ahu_sat_f, +vav_valve_positions,
│   │                                          +vav_zone_temps_f; Meter B rewired to sum per-VAV
│   ├── ahu_physics.py                         NEW — pure-function AHU physics
│   ├── vav_physics.py                         NEW — pure-function VAV physics
│   └── devices/
│       ├── emon.py                            unchanged
│       ├── onicon_gas.py                      unchanged
│       ├── onicon_water.py                    unchanged
│       ├── ahu.py                             NEW — 18-point AHU wrapper (AV/BV)
│       └── vav.py                             NEW — 9-point VAV wrapper (AV/BV)
├── scripts/
│   ├── verify_meter_a.py                      (legacy, retained)
│   ├── verify_meters_abc.py                   Phase 1/2 regression — 4/4 PASS
│   ├── verify_gas_water.py                    Phase 3 regression — 7/7 PASS
│   ├── verify_ahu.py                          NEW — Phase 4 — 28/28 PASS
│   ├── verify_vav.py                          NEW — Phase 5 — 28/28 PASS
│   └── add_loopback_ips.bat                   unchanged
├── reference/                                 unchanged (pattern source only)
├── ops/
│   ├── sprint-tracker.md                      updated — Phases 4+5 ✓
│   ├── action-items.md                        updated — Session C carry-forward
│   ├── Work-Report-2026-04-15.md              Session A
│   ├── Work-Report-2026-04-15-B.md            NEW — Session B
│   ├── Handoff-2026-04-15.md                  Session A → B
│   └── Handoff-2026-04-15-B.md                NEW — Session B → C (this file)
├── logs/                                      (gitignored)
├── site_config.json                           +ahus[] (3 rows), +vavs[] (20 rows)
├── requirements.txt                           BAC0==2025.9.15 (unchanged)
├── README.md                                  one-line placeholder (Session C target)
├── PROJECT_CONTEXT.md                         (from Session A)
└── .gitignore                                 unchanged
```

### Loopback adapter
- Adapter name: `Bacnet Simulator` (index 11) on dev box
- Assigned: `192.168.100.1`–`.11` (Phase 1 residual, unused) + `192.168.100.200`–`.228` (ship range, active)
- Used this session: `.200`–`.228` = 28 devices + verify client on `.228`
- Ship range exactly matches 28 devices + 1 Workbench/JACE IP

### Architectural invariants (all honored)
1. ✅ Five device classes total: `EmonClass3200`, `OniconF5500Gas`, `OniconF3500Water`, `AHU`, `VAV` — complete.
2. ✅ Physics in pure functions: `site_model.py`, `ahu_physics.py`, `vav_physics.py`. Device classes are thin BACnet wrappers with no physics logic.
3. ✅ Per-instance uniqueness config-driven: AHU config rows in `site_config.json ahus[]`; VAV config rows in `vavs[]` (parent_ahu, position, zone_phase_deg).
4. ✅ Single `site_config.json` for all tunables.
5. ✅ One `simulator/__main__.py` entry.
6. ✅ Port 47808 only; one IP per device; sequential `.200`–`.227` for devices, `.228` for JACE/Workbench.

## Session C scope (2026-04-17, Fri) — Phase 6 ship packaging

**Goal:** Zip bundle co-worker can unzip and run. No new simulator code.

### Tasks
1. **`run.bat`** (repo root)
   - If `.venv` missing, create it: `py -m venv .venv`
   - Activate and install: `.venv\Scripts\python.exe -m pip install -r requirements.txt`
   - Launch: `.venv\Scripts\python.exe -m simulator`
   - Keep console open on exit for error review

2. **`README.md`** rewrite (co-worker-facing)
   - **Prerequisites**: Python 3.12 or later; Microsoft KM-TEST Loopback adapter installed; network adapter named (configurable — Jake's is "Bacnet Simulator")
   - **Setup (once)**: assign IPs `.200`–`.228` to the loopback adapter via `netsh interface ipv4 add address name="<adapter>" 192.168.100.NNN 255.255.255.0` — provide copyable block for 29 IPs
   - **Config**: edit `site_config.json` — change `network.base_ip` and `network.start_octet` to match co-worker's network; optionally rename adapter placeholders
   - **Run**: double-click `run.bat`. Expect ~60s bring-up for 28 devices (2s/device is normal).
   - **Troubleshoot**: port 47808 conflicts (another BACnet app running?), elevated needed for netsh, log file at `logs/simulator.log`
   - **Stop**: Ctrl+C in console

3. **Ship-artifact assembly**
   - Decide: `.gitattributes export-ignore` approach vs explicit zip build script
   - Ship includes: `simulator/`, `site_config.json`, `requirements.txt`, `run.bat`, `README.md`, empty `logs/` placeholder
   - Ship excludes: `reference/`, `scripts/`, `ops/`, `.claude/`, `PROJECT_CONTEXT.md`, `.gitignore`, `.git/`

4. **Smoke test**
   - Unzip to scratch dir (e.g. `C:\temp\bacnet-bounty-test\`)
   - Double-click `run.bat`
   - Confirm venv creation, install, 28 devices online, tick loop steady
   - Read one point via BACnet client (manual or via a verify script copied in ad-hoc)
   - Clean teardown with Ctrl+C

5. **Commit**: `Phase 6: ship packaging — run.bat, README, zip assembly`

### Session C prerequisites (none blocking)
- All verify scripts green — ✅
- Loopback range fully provisioned — ✅ (`.200`–`.228`)
- Spec conflicts resolved in docs 03/04 — ✅
- No pending code changes — ✅

## Known items carried forward

From `ops/action-items.md` — none of these block Session C:

- **[Spec docs] Amend 01/02 in-place with Phase 2 + Session B resolutions** (doc 04 Flags A–E). Not blocking; cleanup task for Jake at convenience.
- **[Legacy] `scripts/verify_meter_a.py`** disposition — Session C decides delete vs keep as historical artifact.
- **[Dev box] Python 3.14 vs ship-target 3.12** — monitor. Not hit yet.
- **[Loopback] Old Phase 1 IPs `.1`–`.11`** on `Bacnet Simulator` adapter — unused. Session C README can instruct co-worker to clean up or ignore.
- **[Adapter name]** `Bacnet Simulator` is Jake-specific — README Session C must use configurable placeholder.
- **[Phase 7] Tuning pass triggers** — post-ship, after 2–3 days of Niagara history.
- **[Metro naming]** AHU/VAV point names follow spec, not Metro abbreviations. Visibility only — no action unless co-worker's Niagara slot sheets need realignment.
- **[Bring-up time]** ~60s for 28 devices (2s/device). README must set co-worker expectation so they don't kill the process prematurely.
- **[bacpypes3 vendor warnings]** — filter verified working at 28 devices. No action.

## Readiness checklist for Session C

- [x] Session B commits pushed (will push at end of this session)
- [x] All 28 devices on wire
- [x] 67/67 verify checks passing
- [x] 5 device classes complete — no new device code in Session C
- [x] Architecture lined up for zip packaging (clear separation between ship artifact `simulator/` and dev-time `reference/` + `scripts/` + `ops/`)
- [x] Warnings filter working
- [x] Loopback adapter has full ship range provisioned
